import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#191B41',
        alignItems: 'center',
        justifyContent: 'center',
    },
});

export default style;